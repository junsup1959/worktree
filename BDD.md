# (Behavior-Driven Development: 행동 주도 개발)

## 주의사항 : 개발을 위한 테스트 주도 개발 스킬이므로 전체를 아우르는 큰 맥락을 이루는 시나리오는 금지한다.

## BDD 방법론 개발 시나리오 명세서(Feature)를 작성하며 시작합니다.

## BDD 테스트
- 주체(Actor) : 누구인지에 따라 시나리리오를 작성한다.
- 입력(When) : 주체(Actor) 쪽에서 코드 가장 밖에서 입력을 하였을때 기준으로 테스트를 진행한다. -사용자의 버튼 클릭 / APi 서버의 경우 Http Reqeust/ CLI 및 시스템 라이브러리 의 경우 명령어, 시스템 및 커널 API 가 전달 해주시는 파라미터 등
- Red : 시나리오는 항상 실패하는 기준을 만든다.
- Green : 은 실패한 것을 바탕으로 수정하여 다시 Red로 돌아갔을 경우 성공 하도록 수정한다.
- Refactor : 모든 시나리오 통과 후 작성한 코드의 정리를 한다.

| 구분 | 1. 사용자 기준 (UI) | 2. API / 백엔드 기준 | 3. 시스템 App / CLI 기준 |
| :--- | :--- | :--- | :--- |
| **주체 (Actor)** | 사람 사용자 (User) | 프론트엔드 / 타 백엔드 서버 | 개발자 / OS / 타 라이브러리 |
| **입력 (When)** | 버튼 클릭, 폼 입력, 스와이프 | HTTP Request, Kafka 메시지 | CLI 명령어, 파라미터, 함수 호출 |
| **Red (실패)** | UI 화면에 결과 미출력 | HTTP 404/500 또는 잘못된 JSON | Exit Code 오류, 잘못된 STDOUT/리턴값 |
| **Green (방어)** | 버튼 이벤트 처리 & UI 컴포넌트 렌더링 | 컨트롤러/서비스 로직 구현 & DB 반영 | CLI 인자 파싱 & 핵심 계산/스트림 처리 |
| **Refactor** | UI 컴포넌트 재사용화, CSS 정돈 | DB 쿼리 최적화, 레이어드 아키텍처 정돈 | 메모리/버퍼 성능 최적화, 모듈화 |
