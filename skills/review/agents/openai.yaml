interface:
  display_name: "Review"
  short_description: "Verify intent and find evidence-backed defects"
  default_prompt: "Use $review to verify that the code matches the user's stated intent and acceptance criteria, then report evidence-backed defects and limitations."
